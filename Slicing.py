# #Dictionary Methods:

# clear(): Removes all items from the dictionary.


# d.clear()
# get(key, default=None): Returns the value for a key if it exists, else returns default.


# d.get('key')
# items(): Returns a view object of (key, value) pairs.


# d.items()
# keys(): Returns a view object of keys.


# d.keys()
# values(): Returns a view object of values.


# d.values()
# update([other]): Updates dictionary with items from another dictionary or key-value pairs.


# d.update({'a': 2})
# pop(key, default=None): Removes specified key and returns the value.


# d.pop('a')
# popitem(): Removes and returns the last key-value pair.


# d.popitem()
# setdefault(key, default=None): Returns value of a key; if key doesn’t exist, inserts it with default value.


# d.setdefault('b', 10)
# copy(): Returns a shallow copy of the dictionary.

# d.copy()
# fromkeys(seq, value=None): Creates a new dictionary from a sequence of keys and a common value.


# dict.fromkeys(['a', 'b'], 0)

